# 辅助功能授权引导验收

范围：Spoken 2.2.1 / 220.1 的首次授权引导、权限状态与后续提醒，以及未授权时的文本回退。此前快捷键迭代仍包含在当前工作区中。

## 验证方式

- 编译实际应用源码，通过注入权限检查、设置入口、临时配置、独立剪贴板及模拟粘贴事件进行回归。
- 未请求或更改本机辅助功能授权，未读取 TCC 数据库，不录音，不调用真实模型。
- 新增 15 组回归：未授权启动、跳过后的重启、设置点击不等于授权、入口失败、授权与撤销、事件权限未生效、检测生命周期、定位应用、界面依赖隔离，以及文本回退和剪贴板恢复。
- 原生界面覆盖首次引导、权限设置、最小模式面板、输出提醒和录音浮窗的深浅色布局。

## 执行结果

- 离线回归 **111/111 通过**（原有 96 组 + 本次 15 组），没有外部模型请求。
- 原生界面 **28 张**渲染成功，检查了授权引导、权限页、最小面板、输出提示与录音浮窗。渲染检查确认前台应用未改变。
- 隔离应用交互冒烟通过：未授权 → 模式面板提醒 → 进入权限页；点击模拟设置入口及重新检测不误报成功；入口失败显示手动路径；展开更新/重新添加应用说明；模拟授权后提示消失；模拟权限失效后提示恢复；模拟录音显示手动粘贴说明，取消后正常返回。检查了可访问性标签，未进行真实 VoiceOver 朗读验收。小屏导航的待授权状态改用紧凑警示图标，同时保留“待授权”的完整无障碍标签。测试应用已退出。
- 本地构建 **2.2.1 / 220.1** 完成，签名严格校验通过。随后按用户要求替换本机版本，安装结果见下文。
- 首次沙箱执行无法访问 macOS 剪贴板服务，末项测试因此没有恢复标识并提前终止，随后补上明确的失败反馈，避免强制解包。将同一组测试放到允许访问图形服务的环境中复核，111/111 通过；仍使用独立命名剪贴板和模拟权限，不涉及通用剪贴板或真实授权。

命令：

```bash
bash scripts/run_offline_ai_tests.sh --build-ui-smoke
# 为避免应用入口自动进入交互窗口，将已编译的测试程序复制到普通目录后运行：
mkdir -p build/accessibility-validation
cp build/SpokenSmoke.app/Contents/MacOS/SpokenSmoke build/accessibility-validation/offline-tests
build/accessibility-validation/offline-tests --render-ui build/accessibility-ui
bash scripts/build_local_app.sh
```

独立剪贴板及原生窗口检查需要允许访问 macOS 图形服务。无此权限的执行沙箱不能作为剪贴板行为的验收环境。

## 人工验收边界

真实系统设置开启/撤销权限、替换应用后的系统授权行为，以及真实目标应用的粘贴仍需使用者验收。测试中的“已授权”是注入状态，粘贴成功是模拟事件发送成功，均不等同于实际输入框已收到文字。前次快捷键迭代的实体键盘测试继续保留待验证。

## 交付

- `build/Spoken.app`、`build/Spoken-2.2.1-macos-arm64.zip`：本地签名的 Apple Silicon 构建。ZIP 完整性及所有包内文件与签名构建的一致性检查通过。
- ZIP SHA-256：`7123ea17081def694e02c6c5bef2ecec8bd0faf7964777dda3a74871beaf3843`。
- 安装包、渲染图和隔离测试应用位于被忽略的 `build/`，未纳入公开源码。

## 本机替换安装

- 从 2.2.0 / 219.1 升级至 **2.2.1 / 220.1**，正常退出旧进程后替换并启动新版。
- 安装后的所有包内文件与已验证构建一致，签名严格校验通过，新进程运行路径为 `/Applications/Spoken.app/Contents/MacOS/Spoken`。
- 现有 4 份配置文件、19 个应用偏好均与替换前一致；没有导出钥匙串或修改系统授权。
- 旧应用、配置和安装核验记录保存在被忽略的 `build/backups/` 私有目录。未保存运行日志、语音或转录正文。
- 此次安装未进行真实录音、粘贴或系统授权切换测试，人工验收边界不变。
