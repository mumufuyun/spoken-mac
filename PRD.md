# Spoken 产品需求

> Last updated: 2026-08-24

## 产品定位

Spoken 是 macOS 菜单栏语音输入工具：将语音按真实使用场景整理成文字，并输入录音前聚焦的 Mac 应用。

核心目标：

- 云端 ASR 在长时间运行、休眠唤醒和短暂断网后仍可恢复。
- 用户选择“文字要用在哪里”，而不是抽象的 AI 处理动作。
- 默认保持原意、不虚构事实，快速注入目标窗口。

## 核心流程

```text
快捷键 / 菜单栏 → 录音 → 云端 ASR（可降级本地）
  → 按场景后处理 → 可选输出语言 → 注入原目标应用
```

## 使用场景

| 场景 | 目标 |
|---|---|
| 原样转写 | 仅转录，原语言时不调用 AI |
| 日常聊天 | 自然、轻松，保留本人语气 |
| 工作沟通 | 结论与诉求优先，简洁礼貌 |
| 正式材料 | 严谨书面化，根据内容组织层级 |
| 会议记录 | 整理讨论、结论、待办和待确认项 |
| 内容分享 | 增强可读性和节奏，保留真实观点 |
| AI 指令 | 将口述意图整理成可直接交给另一个 AI 的清晰指令，不提前执行任务 |

输出语言与场景解耦：原语言、英文、日文或韩文。

## ASR 稳定性要求

- 每次录音创建新云端会话，不复用长期空闲 WebSocket。
- 建连前音频必须缓存；断线重连后按顺序重放。
- 必须有连接超时、有界重试、心跳和有界收尾。
- 停止后等待最终结果；超时时使用最新部分识别。
- 自动模式永久失败时降级本地；纯云端模式明确报错。

## 隐私与验收

- API Key 只保存在 macOS Keychain，不写入代码、文档或日志。
- 个人背景保存在本机偏好设置中，可编辑、停用或清空；仅在需要 AI 后处理时发送给当前模型服务商。
- 稳定性指标只记录会话、成功、失败、重连和降级计数，不记录音频或文本。
- 仓库只保留 macOS 实现；七个场景、旧设置迁移和 ASR 核心逻辑需有测试。
- 真实网络验证按 [ITERATION_2026_ASR_AND_SCENES.md](ITERATION_2026_ASR_AND_SCENES.md) 执行。
