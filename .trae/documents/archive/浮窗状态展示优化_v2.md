# 浮窗状态展示优化 v2

## 问题梳理

### 当前流程
1. 录音中 → 浮窗显示"录音中" + 实时识别文本
2. 停止录音 → 浮窗直接消失 → 文本注入

### 用户期望流程
1. 录音中 → 浮窗显示"录音中" + 实时识别文本
2. 停止录音 → 浮窗显示当前模式（如"润色模式"）+ "处理中..."
3. AI 处理完成 → 浮窗显示"处理完成"（停留 1 秒）
4. 文本注入 → 浮窗消失

## 状态流转设计

```
录音中 → [模式名]处理中... → 处理完成（停留1s）→ 文本注入中... → 浮窗消失
```

### 状态定义

| 阶段 | displayStatus（右上角） | statusText（底部） |
|------|----------------------|------------------|
| 录音中 | "录音中" | 实时识别文本 |
| AI处理中 | "润色中..." / "翻译中..."等 | ""（空，或显示"AI处理中..."） |
| 处理完成 | "处理完成 ✓"（停留1s） | "" |
| 文本注入 | "文本注入中..." | "" |
| 已完成 | "已完成"（可选，然后消失） | "" |

## 修复方案

### 1. RecordingViewModel 改造

新增 `@Published var displayStatus = ""` 专门用于状态展示：

```swift
class RecordingViewModel: ObservableObject {
    @Published var isRecording = false
    @Published var isProcessing = false
    @Published var partialText = ""
    @Published var statusText = ""        // 识别文本（底部展示）
    @Published var displayStatus = ""      // 状态信息（右上角展示）
    @Published var isCancelled = false
    
    func startRecording() {
        // ...
        displayStatus = "录音中"
        
        SpeechService.shared.startRecording(
            onPartial: { [weak self] text in
                self?.partialText = text
                self?.lastRecognizedText = text
                self?.statusText = text           // 底部展示识别文本
                self?.displayStatus = "录音中"     // 右上角保持状态
            },
            onFinal: { [weak self] text in
                self?.isRecording = false
                self?.isProcessing = true
                self?.partialText = ""
                self?.statusText = ""              // 清空底部文本
                // 根据当前模式设置状态
                let modeRaw = UserDefaults.standard.string(forKey: "spokenMode") ?? "直接输入"
                let mode = SpokenMode(rawValue: modeRaw) ?? .direct
                if mode == .direct {
                    self?.displayStatus = "处理完成"
                } else {
                    self?.displayStatus = "\(mode.rawValue)中..."
                }
                // ...
            }
        )
    }
    
    private func processAndInput(_ text: String) {
        // ...
        if mode == .direct {
            displayStatus = "处理完成"
            isProcessing = false
            onComplete?(text, frontmostApp)
            return
        }
        
        // AI 处理中状态已在 onFinal 中设置
        // 这里保持 displayStatus 不变（如"润色中..."）
        
        MiniMaxService.shared.process(...) { result in
            // ...
            // 处理完成，显示"处理完成"停留1秒
            strongSelf.displayStatus = "处理完成 ✓"
            strongSelf.isProcessing = false
            
            // 延迟1秒后执行文本注入并关闭浮窗
            DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
                strongSelf.onComplete?(finalText, strongSelf.frontmostApp)
            }
        }
    }
}
```

### 2. AppDelegate onComplete 改造

当前 `onComplete` 直接执行注入，需要调整让浮窗先展示"处理完成"再消失：

```swift
viewModel.onComplete = { [weak self] text, appFromViewModel in
    guard let strongSelf = self else { return }
    
    // 状态：文本注入中...
    strongSelf.recordingViewModel.displayStatus = "文本注入中..."
    
    // 延迟执行注入（给用户看到"处理完成"的时间）
    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
        // ... 执行注入逻辑
        
        // 注入完成后关闭浮窗
        strongSelf.recordingPanel?.orderOut(nil)
        strongSelf.recordingPanel = nil
    }
}
```

### 3. UI 改造

右上角状态区使用 `displayStatus`：

```swift
if viewModel.isRecording {
    HStack(spacing: 5) {
        Circle().fill(Color(hex: "#c0392b")).frame(width: 6, height: 6)
            .scaleEffect(pulseScale)
            .animation(...)
        Text(viewModel.displayStatus)  // "录音中"
    }
} else if viewModel.isProcessing {
    HStack(spacing: 5) {
        Circle().fill(Color(hex: "#f39c12")).frame(width: 6, height: 6)
            .scaleEffect(pulseScale)
            .animation(...)
        Text(viewModel.displayStatus)  // "润色中..." / "翻译中..."
    }
} else if viewModel.displayStatus.contains("完成") || viewModel.displayStatus.contains("✓") {
    HStack(spacing: 5) {
        Circle().fill(Color(hex: "#27ae60")).frame(width: 6, height: 6)
        Text(viewModel.displayStatus)  // "处理完成 ✓"
    }
}
```

底部文本区：
- 录音中：显示实时识别文本 `statusText`
- AI 处理中：显示"AI 处理中..."或保持空
- 处理完成：显示"处理完成"或保持空

## 实施步骤

1. RecordingViewModel 新增 `displayStatus` 属性
2. 修改状态流转逻辑，确保每个阶段 displayStatus 正确
3. AI 处理完成后停留 1 秒再执行 onComplete
4. AppDelegate onComplete 增加"文本注入中..."状态
5. UI 右上角改用 displayStatus，底部文本区优化
6. 编译验证
7. 提交代码
